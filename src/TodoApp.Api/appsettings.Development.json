{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",

      // Set to "Information" to see every SQL statement EF Core runs.
      // Noisy, but the fastest way to answer "why is the XP total wrong?".
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  }
}
