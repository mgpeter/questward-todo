# The whole app on one port. Copy .env.example to .env first if you want to change
# the password or the published port.
#   docker compose up -d --build   ->  http://localhost:8080
name: questward

services:
  db:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-questward}
      POSTGRES_USER: ${POSTGRES_USER:-questward}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-questward}
    volumes:
      # Postgres 18 sets PGDATA to /var/lib/postgresql/18/docker, so the volume goes one level up.
      - questward-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-questward} -d ${POSTGRES_DB:-questward}"]
      interval: 5s
      timeout: 5s
      retries: 12

  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      ConnectionStrings__Default: >-
        Host=db;Port=5432;Database=${POSTGRES_DB:-questward};Username=${POSTGRES_USER:-questward};Password=${POSTGRES_PASSWORD:-questward}
    ports:
      - "${QUESTWARD_PORT:-8080}:8080"

volumes:
  questward-data:
